# Required — get one at https://aistudio.google.com (Get API key → Create)
GEMINI_API_KEY=

# Optional overrides
GEMINI_MODEL=gemini-2.5-pro
HOST_PORT=8000
CORS_ORIGINS=https://molu.likelionscnu.site,http://localhost:3000,http://localhost:3300
MAX_IMAGE_MB=10
